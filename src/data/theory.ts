import { Categories } from "./questions";

interface CheatSheetContent {
    [key: string]: string;
}

export const CHEAT_SHEET_CONTENT: CheatSheetContent = {
    [Categories.JavaScript]: `
### 🔹 Замыкания (Closures)
**Определение**: функция "запоминает" своё лексическое окружение — переменные из внешней области видимости, даже после выхода из неё.

**Где используется**:
- Приватные поля (до появления \`#private\`)
- Каррирование, partial application
- Реализация \`debounce\`, \`throttle\`
- Кеширование результатов
- Фабрики функций

**Пример**:
\`\`\`js
function createCounter() {
  let count = 0;
  return () => ++count;
}
const counter = createCounter();
counter(); // 1
counter(); // 2
\`\`\`

⚠️ **Утечки памяти**: замыкание удерживает ссылку на переменные. Не сохраняйте большие объекты или DOM-узлы без очистки.

💡 **Подсказка**: если функция в цикле использует \`var\`, все экземпляры будут ссылаться на одну переменную. Используйте \`let\` или IIFE.

---

### 🔹 Event Loop: Асинхронность в JavaScript
JavaScript однопоточен, но асинхронность достигается через **Event Loop**, который координирует выполнение кода.

#### 🔧 Основные компоненты:
1. **Call Stack** — стек вызовов (синхронный код)
2. **Web APIs** — браузерные/средовые API (setTimeout, fetch, DOM)
3. **Callback Queue**:
   - **Макрозадачи (Macrotasks)**: \`setTimeout\`, \`setInterval\`, \`I/O\`, события
   - **Микрозадачи (Microtasks)**: \`Promise.then\`, \`queueMicrotask\`, \`MutationObserver\`
4. **Event Loop** — проверяет стек и очереди

#### 🔄 Правила выполнения:
- После каждого макровыполнения выполняются **все микрозадачи**.
- Рендеринг страницы происходит **между макрозадачами**.
- Микрозадачи имеют **высший приоритет**.

> ✅ **Порядок**:  
> Синхронный код → Микрозадачи → Макрозадача → Рендеринг → Следующая макрозадача

#### 💡 Пример:
\`\`\`js
console.log('1');
setTimeout(() => console.log('2'), 0);
Promise.resolve().then(() => console.log('3'));
console.log('4');
// → 1, 4, 3, 2
\`\`\`

#### ⚠️ Подводные камни:
- Бесконечные микрозадачи блокируют рендеринг:
  \`\`\`js
  Promise.resolve().then(function loop() {
    // рекурсия без 'дыхания'
    Promise.resolve().then(loop);
  });
  \`\`\`
- В Node.js \`process.nextTick\` приоритетнее \`Promise.then\`.

#### 🛠 Полезные практики:
- Используйте \`queueMicrotask()\` для чистого доступа к микрозадачам.
- Для отложенного выполнения без блокировки: \`setTimeout(fn, 0)\` или \`postMessage\`.
- Отменяйте асинхронные операции через \`AbortController.signal\`.

💡 **Запомните**:  
> "**Микрозадачи запускаются до следующей макрозадачи, даже если таймер 0.**"
`,

    [Categories.TypeScript]: `
### 🔹 Типизация и рантайм
**TS работает только на этапе разработки** → типы исчезают при компиляции. Данные с API нельзя доверять.

❌ Плохо:
\`\`\`ts
fetch('/api/user').then(r => r.json() as User); // Небезопасно!
\`\`\`

✅ Решения:
- **Type guards**: \`if ('name' in obj) ...\`
- **Zod / Yup**: валидация на рантайме
- **Ручные проверки + fallback**

### 🔹 interface vs type
|          | \`interface\`                     | \`type\`                          |
|--------|----------------------------------|----------------------------------|
| Расширение | \`extends\`                       | \`&\`                             |
| Слияние | Да (\`declaration merging\`)       | Нет                              |
| Union   | Нет                              | Да: \`type A = B \| C;\`         |
| Условные | Ограничено                       | Полная поддержка                 |

**Рекомендации**:
- \`interface\`: для объектов, классов, API
- \`type\`: для union, conditional, mapped типов, литералов

### 🔹 Generics и утилиты
**Generics** позволяют писать переиспользуемый код:
\`\`\`ts
function identity<T>(arg: T): T { return arg; }
\`\`\`

**Утилиты**:
- \`Partial<T>\` — все поля опциональны
- \`Required<T>\` — все обязательны
- \`Pick<T, K>\` — выбрать поля
- \`Omit<T, K>\` — исключить поля
- \`Record<K, T>\` — создать объект с ключами типа K
- \`ReturnType<typeof fn>\` — вывести тип возврата

💡 Совет: используйте \`as const\` для предотвращения widening массивов и объектов.
`,

    [Categories.React]: `
### 🔹 Ререндеры и производительность
React сравнивает пропсы **по ссылке** → новый объект/функция = ререндер.

**Оптимизации**:
- \`React.memo(Component)\` — мемоизация компонента
- \`useMemo(() => expensive(), [deps])\` — кэширование значений
- \`useCallback(fn, deps)\` — мемоизация функций

⚠️ **Избыточная мемоизация** может навредить: накладные расходы на сравнение > выгода.

### 🔹 useState vs useReducer
- \`useState\`: простые значения (строки, числа, булевы)
- \`useReducer\`: сложное состояние (объекты с несколькими полями, формы, state machines)

✅ Подходит для: форм, шаговых процессов, управления состоянием с чёткой логикой переходов.

### 🔹 useRef
- Хранит изменяемое значение между ререндерами
- Не вызывает перерисовку
- \`ref.current\` доступен сразу

**Использование**:
- Доступ к DOM: \`<div ref={ref} />\`
- Хранение предыдущего значения (через \`useEffect\`)
- Отмена асинхронных операций: \`timeoutId\`, \`AbortController\`

💡 \`useRef\` ≠ \`useState\`: он не триггерит ререндер.

### 🔹 Ключи (key)
Ключ помогает React идентифицировать элементы при обновлении списка.

❌ Избегайте \`index\` как ключа, если:
- Список можно переупорядочивать
- Есть фильтрация
- Элементы добавляются/удаляются в середине

✅ Всегда используйте **уникальный ID** (например, \`id\` из бэкенда).
`,

    [Categories.CSS]: `
### 🔹 Производительность CSS
**Repaint**: перерисовка (цвет, фон).  
**Reflow/Layout**: пересчёт геометрии (ширина, высота).  
**Compositing**: работа с GPU-слоями.

✅ **Анимируйте только**:
- \`transform\`
- \`opacity\`

🚫 Избегайте анимации \`width\`, \`height\`, \`margin\` — они вызывают reflow.

**Оптимизации**:
- \`will-change: transform\` — подсказка браузеру (не злоупотреблять!)
- \`contain: strict\` — изолирует layout/style/paint
- \`content-visibility: auto\` — отложенная отрисовка внедашнего контента

### 🔹 Центрирование
1. **Flexbox**:
   \`\`\`css
   display: flex;
   align-items: center;
   justify-content: center;
   \`\`\`
2. **Grid**:
   \`\`\`css
   display: grid;
   place-items: center;
   \`\`\`
3. **Absolute + transform**:
   \`\`\`css
   position: absolute;
   top: 50%; left: 50%;
   transform: translate(-50%, -50%);
   \`\`\`

💡 Flex и Grid — современные и надёжные решения. Transform — для фиксированных позиций.

### 🔹 Адаптивность
**Mobile-first**:
- Базовые стили — для мобильных
- Через \`min-width\` добавляем правила для планшетов и десктопов

**Единицы**:
- \`rem\`: относительно корня (\`html\`)
- \`em\`: относительно родителя
- \`clamp()\`: резиновый шрифт: \`font-size: clamp(1rem, 2.5vw, 2rem);\`

**Container Queries**:
Позволяют реагировать на размер контейнера, а не viewport:
\`\`\`css
.card { container-type: inline-size; }
@container (min-width: 300px) { .card { ... } }
\`\`\`

### 🔹 Доступность (a11y)
- Семантика: \`<nav>\`, \`<main>\`, \`<button>\` вместо \`<div onClick>\`
- \`alt\` для изображений
- \`aria-*\`, \`role\` — для скринридеров
- \`focus-visible\` — outline только при клавиатурной навигации
- \`@media (prefers-reduced-motion)\` — отключите анимацию при запросе пользователя
`,

    [Categories.HTTP]: `
### 🔹 HTTP vs HTTPS
|          | HTTP               | HTTPS                     |
|--------|--------------------|---------------------------|
| Порт   | 80                 | 443                       |
| Шифрование | Нет              | TLS/SSL                   |
| Безопасность | Уязвим к MITM | Защищён                   |

**TLS handshake**:
1. Клиент предлагает алгоритмы
2. Сервер выбирает, отправляет сертификат
3. Обмен ключами (ECDHE)
4. Установка зашифрованного соединения

### 🔹 CORS
Политика безопасности браузера, блокирующая запросы с другого origin.

**Preflight (OPTIONS)**:
- Отправляется для не-simple запросов (\`PUT\`, \`DELETE\`, кастомные заголовки)
- Сервер должен ответить:
  - \`Access-Control-Allow-Origin\`
  - \`Access-Control-Allow-Methods\`
  - \`Access-Control-Allow-Headers\`
- При \`credentials: true\` — нельзя использовать \`*\`, нужен конкретный origin

### 🔹 HTTP-статусы
- **2xx**: успех (\`200 OK\`, \`201 Created\`)
- **3xx**: редирект (\`301 Moved Permanently\`)
- **4xx**: ошибка клиента (\`400 Bad Request\`, \`401 Unauthorized\`, \`403 Forbidden\`, \`404 Not Found\`)
- **5xx**: ошибка сервера (\`500 Internal Server Error\`)
`,

    [Categories.Storage]: `
### 🔹 localStorage vs sessionStorage vs cookies
|          | localStorage     | sessionStorage | cookies             |
|--------|------------------|----------------|---------------------|
| Жизнь   | Постоянная       | До закрытия вкладки | До expiry           |
| Размер  | ~5–10 МБ         | ~5 МБ          | ~4 КБ               |
| Отправка | Нет             | Нет            | Да (с каждым запросом) |
| XSS     | Уязвим            | Уязвим         | \`HttpOnly\` — защищён |
| CSRF    | Нет              | Нет            | \`SameSite\` — защита |

**Флаги cookie**:
- \`HttpOnly\`: недоступен через JS
- \`Secure\`: только по HTTPS
- \`SameSite=Strict/Lax\`: защита от CSRF

### 🔹 Кэширование
**Cache-Control**:
- \`max-age=3600\` — кэш на час
- \`no-cache\` — проверка через ETag
- \`no-store\` — не кэшировать
- \`immutable\` — для хешированных файлов

**ETag**: уникальный хеш ресурса. При повторном запросе: \`If-None-Match\` → сервер отвечает 304, если не изменился.

**Service Worker**:
- Перехватывает запросы
- Кэширует ресурсы (\`Cache API\`)
- Позволяет работать офлайн
`,

    [Categories.Build]: `
### 🔹 Этапы CI/CD
1. **Линтинг** (ESLint) → 2. **Тесты** (unit, e2e) → 3. **Сборка** → 4. **Деплой на staging** → 5. **Продакшн**

**Важно**:
- Автоматические тесты перед деплоем
- Healthcheck после деплоя
- Rollback при ошибках

### 🔹 Оптимизация загрузки
- **Tree-shaking**: удаление неиспользуемого кода (работает с ESM)
- **Code splitting**: разделение бандлов
- **Lazy loading**: \`React.lazy(() => import('./Component'))\` + \`Suspense\`
- **Differential bundling**: отдельные сборки для старых и новых браузеров
- **Preload/Prefetch**: \`<link rel="preload">\`, \`webpackPreload\`

**Инструменты анализа**:
- Webpack Bundle Analyzer
- Lighthouse
- Chrome DevTools Coverage
`,

    [Categories.Tests]: `
### 🔹 React Testing Library
**Основные методы**:
- \`getBy...\`: ищет сразу, падает, если нет
- \`queryBy...\`: возвращает null, если нет
- \`findBy...\`: ждёт асинхронно (Promise), подходит для данных

**Правило**: тестируйте так, как пользователь — по тексту, роли, плейсхолдеру.

### 🔹 Паттерны тестирования
- **MSW (Mock Service Worker)**: мокинг API на уровне сети
- **Wrapper**: оборачивайте компонент в провайдеры (Redux, Theme, Auth)
- **Кастомный render**: переиспользуйте wrapper во всех тестах
- **Cleanup**: всегда вызывайте \`cleanup\` или используйте \`afterEach\`

💡 Избегайте хрупких тестов: не завязывайтесь на внутреннюю реализацию.
`,

    [Categories.Api]: `
### 🔹 Работа с fetch
\`\`\`js
try {
  const response = await fetch('/api/data', { signal });
  if (!response.ok) throw new Error('Ошибка сети');
  const data = await response.json();
} catch (e) {
  if (e.name === 'AbortError') console.log('Запрос отменён');
}
\`\`\`

⚠️ \`fetch\` **не отклоняет промис** при 4xx/5xx → всегда проверяйте \`response.ok\`.

✅ Всегда используйте \`signal\` из \`AbortController\` для отмены.

### 🔹 Idempotency-Key
Заголовок, гарантирующий, что повторный запрос не создаст дубликат.

**Как работает**:
1. Клиент генерирует UUID
2. Отправляет с запросом: \`Idempotency-Key: abc-123\`
3. Сервер запоминает ключ и результат
4. При повторе — возвращает тот же ответ

✅ Критично для: платежей, заказов, неидемпотентных операций.

### 🔹 Retry & Backoff
Для нестабильных сетей:
- **Exponential backoff**: задержка увеличивается: 1s, 2s, 4s...
- **Jitter**: случайная вариация, чтобы избежать "шторма" запросов
- **Max retries**: ограничьте количество попыток
`,
};